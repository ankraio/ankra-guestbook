apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: guestbook-runner-test
  labels:
    app.kubernetes.io/name: guestbook-runner-test
  annotations:
    ankra.io/generated: "true"
    cert-manager.io/cluster-issuer: ${{ ankra.ingress_tls_issuer }}
spec:
  ingressClassName: ${{ ankra.ingress_class }}
  tls:
    - hosts:
        - ${{ ankra.ingress_host }}
      secretName: guestbook-runner-test-tls
  rules:
    - host: ${{ ankra.ingress_host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: guestbook-runner-test
                port:
                  number: 80
