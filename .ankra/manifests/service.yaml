apiVersion: v1
kind: Service
metadata:
  name: guestbook-flowtest
  labels:
    app.kubernetes.io/name: guestbook-flowtest
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: guestbook-flowtest
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
