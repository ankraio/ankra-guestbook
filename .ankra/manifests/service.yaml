apiVersion: v1
kind: Service
metadata:
  name: guestbook-runner-test
  labels:
    app.kubernetes.io/name: guestbook-runner-test
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: guestbook-runner-test
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
